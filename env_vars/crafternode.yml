---

# web --------------------------------------------------------------------------
masterfacts: "{{ hostvars[groups['masterservers'][0]] }}"
address: "{%if masterfacts.get('ansible_enp1s3') and masterfacts['ansible_enp1s3']['active']%}{{masterfacts['ansible_enp1s3']['ipv4']['address']}}{%else%}{{masterfacts['ansible_enp0s3']['ipv4']['address']}}{%endif%}"
rabbitmq_host: "{{address}}"
db_host: "{{address}}"
gridftp_user: "{{ web_user }}"
# celery -----------------------------------------------------------------------
celery_num_workers: 4

cluster_host: hornet.hww.de
