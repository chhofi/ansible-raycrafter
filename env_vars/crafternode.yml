---

# web --------------------------------------------------------------------------
masterfacts: "{{ hostvars[groups['masterservers'][0]] }}"
address: "{%if masterfacts.get('ansible_eth1') and masterfacts['ansible_eth1']['active']%}{{masterfacts['ansible_eth1']['ipv4']['address']}}{%else%}{{masterfacts['ansible_eth0']['ipv4']['address']}}{%endif%}"
rabbitmq_host: "{{address}}"
db_host: "{{address}}"
gridftp_user: "{{ web_user }}"
# celery -----------------------------------------------------------------------
celery_num_workers: 4

cluster_host: hornet.hww.de
